# Model files are never committed (served locally in dev, from R2 in production)
models/*.onnx

node_modules/
dist/

# ORT runtime files are served from node_modules in dev, R2 in production
public/ort/
# Staged R2 uploads (scripts/prepare-ort-upload.mjs); node_modules is the source
r2-upload/

# Test artefacts
test-results/
playwright-report/
coverage/

*.local
.DS_Store

*.tsbuildinfo

.bench-profile/

.bench-profile-gpu/
